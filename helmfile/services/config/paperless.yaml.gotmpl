---
# paperless: see ../helmfile.yaml.gotmpl for what these keys mean.
releaseConfig:
  paperless:
    routes:
      main:
        networks: [lan, guest, vpn_main, vpn_guest, vpn_family]
    secrets: [ses/incoming, redis, admin, secret-key]
