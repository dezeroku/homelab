---
# change-detection: see ../helmfile.yaml.gotmpl for what these keys mean.
releaseConfig:
  change-detection:
    chart: bjw-s-labs/app-template
    version: 5.0.1
    templates: [gateway, oidc-secret]
    routes:
      main:
        networks: [lan, guest, vpn_main]
        oidc: true
